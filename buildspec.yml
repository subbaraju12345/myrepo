version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - cd
      - ls
  post_build:
    commands:
      
artifacts:
  files:
    - target/messageUtil-1.0.jar
